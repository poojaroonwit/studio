import type { Applicant, Position, UserProfile } from './types';

// Note: All mock data has been removed. Use the database for real data.
// The system now relies on PostgreSQL database for all data storage.
    
