# =================================================================
# Internal Network Deployment Configuration
# =================================================================
# This file contains environment variables for self-hosting on internal network



# =================================================================
# Application Configuration
# =================================================================
NODE_ENV=production
APP_PORT=8021
NEXT_TELEMETRY_DISABLED=1
APP_TIMEZONE=Asia/Bangkok



# =================================================================
# Database Configuration (PostgreSQL)
# =================================================================
# For local development, use localhost:8521
# For internal network deployment, use postgres:5432
POSTGRES_USER=CHANGE_THIS_POSTGRES_USER
POSTGRES_PASSWORD=CHANGE_THIS_POSTGRES_PASSWORD
POSTGRES_DB=studio_db
POSTGRES_PORT=8521
POSTGRES_HOST=localhost
DATABASE_URL=postgresql://CHANGE_THIS_POSTGRES_USER:CHANGE_THIS_POSTGRES_PASSWORD@localhost:8521/studio_db

# Database SSL Configuration
# SECURITY: Set to true to enable SSL for database connections
DATABASE_SSL=false  # Set to true for production
# SECURITY: Set to true to validate SSL certificates (recommended)
# Set to false only if using self-signed certificates
DATABASE_SSL_REJECT_UNAUTHORIZED=true  # Default: true (secure). Set to false only for self-signed certs

# Internal network Docker settings (uncomment for internal deployment)
# POSTGRES_PORT=5432
# POSTGRES_HOST=postgres
# DATABASE_URL=postgresql://CHANGE_THIS_POSTGRES_USER:CHANGE_THIS_POSTGRES_PASSWORD@postgres:5432/studio_db



# =================================================================
# MinIO Object Storage Configuration
# =================================================================
# For local development, use localhost
# For internal network deployment, use minio
MINIO_ROOT_USER=CHANGE_THIS_MINIO_USER
MINIO_ROOT_PASSWORD=CHANGE_THIS_MINIO_PASSWORD
MINIO_ENDPOINT=localhost
MINIO_PORT=8621
MINIO_ACCESS_KEY=CHANGE_THIS_MINIO_ACCESS_KEY
MINIO_SECRET_KEY=CHANGE_THIS_MINIO_SECRET_KEY
MINIO_BUCKET_NAME=studio-files
MINIO_USE_SSL=false
MINIO_PUBLIC_BASE_URL=http://localhost:8721
MINIO_API_PORT=8621
MINIO_CONSOLE_PORT=8721

# Internal network Docker settings (uncomment for internal deployment)
# MINIO_ENDPOINT=minio
# MINIO_PORT=9000
# MINIO_PUBLIC_BASE_URL=http://localhost:9847
# MINIO_API_PORT=9847
# MINIO_CONSOLE_PORT=9848



# =================================================================
# Authentication Configuration
# =================================================================
# Generate a secure secret: openssl rand -base64 32
NEXTAUTH_SECRET=CHANGE_THIS_GENERATE_SECURE_SECRET_USING_OPENSSL
NEXTAUTH_URL=http://localhost:8021


# =================================================================
# CORS Configuration (Security)
# =================================================================
# Comma-separated list of allowed origins for CORS requests
# Supports wildcard patterns like *.qsncc.com (all subdomains)
# Example: http://localhost:8021,http://192.168.1.37:8021,*.qsncc.com
# Note: *.qsncc.com is automatically allowed for all subdomains
# SECURITY: Only include trusted domains to prevent unauthorized access
# 
# Azure AD SSO Note: Azure AD domains are automatically added when Azure AD is configured.
# Azure AD SSO typically uses redirects (not CORS), so explicit CORS configuration
# is usually not required for SSO. However, Azure AD domains are automatically
# allowed if AZURE_AD_CLIENT_ID, AZURE_AD_CLIENT_SECRET, and AZURE_AD_TENANT_ID are set.
CORS_ALLOWED_ORIGINS=http://localhost:8021,*.qsncc.com



# =================================================================
# Azure AD Configuration (Optional - for SSO)
# =================================================================
# If you want to use Azure AD for authentication, uncomment and configure these
# AZURE_AD_CLIENT_ID=your_azure_ad_client_id
# AZURE_AD_CLIENT_SECRET=your_azure_ad_client_secret
# AZURE_AD_TENANT_ID=your_azure_ad_tenant_id
# NEXT_PUBLIC_AZURE_AD_CLIENT_ID=your_azure_ad_client_id
# NEXT_PUBLIC_AZURE_AD_TENANT_ID=your_azure_ad_tenant_id



# =================================================================
# External API Keys (Optional)
# =================================================================
# Google API Key for fonts and other Google services
# GOOGLE_API_KEY=CHANGE_THIS_YOUR_GOOGLE_API_KEY
# NEXT_PUBLIC_GOOGLE_FONTS_API_KEY=CHANGE_THIS_YOUR_GOOGLE_API_KEY



# =================================================================
# Webhook Configuration (Optional)
# =================================================================
# URLs for external webhook processing (if using external resume processing)
# WARNING: Do not include API keys in URLs. Use tokens in headers instead.
RESUME_PROCESSING_WEBHOOK_URL=CHANGE_THIS_YOUR_WEBHOOK_URL
RESUME_PROCESSING_WEBHOOK_TOKEN=CHANGE_THIS_YOUR_WEBHOOK_TOKEN
GENERAL_PDF_WEBHOOK_URL=CHANGE_THIS_YOUR_WEBHOOK_URL
GENERAL_PDF_WEBHOOK_TOKEN=CHANGE_THIS_YOUR_WEBHOOK_TOKEN
RESUME_PROCESSING_WEBHOOK_TIMEOUT=1800
WEBHOOK_CONNECTION_TIMEOUT=900
PROCESSOR_API_KEY=CHANGE_THIS_YOUR_PROCESSOR_API_KEY
PROCESSOR_URL=http://8021_fitscan_app:8021
# Upload Queue Processor Configuration - FIXED TIMING
PROCESSOR_INTERVAL_MS=5000           # Fixed interval: 5000ms (5 seconds) - no dynamic adjustments
MAX_CONCURRENT_PROCESSORS=1          # Default to 1 for connection optimization
LOG_INTERVAL_MS=5000                 # Status logging interval: 5000ms (5 seconds)
UPLOAD_QUEUE_PROCESS_URL=http://8021_fitscan_app:8021/api/upload-queue/process



# =================================================================
# Real-time Configuration (SSE)
# =================================================================
# SSE is used for real-time updates instead of WebSocket
# No additional configuration needed for SSE



# =================================================================
# Public MinIO Configuration (Client-side accessible)
# =================================================================
NEXT_PUBLIC_MINIO_BUCKET=studio-files
NEXT_PUBLIC_MINIO_PUBLIC_BASE_URL=http://localhost:9847



# =================================================================
# Internal Network Configuration
# =================================================================
# Note: INTERNAL_SERVER_IP and INTERNAL_DOMAIN are not used in the application


# =================================================================
# Sentry Configuration (Optional - for error tracking)
# =================================================================
# Get your DSN from https://sentry.io/settings/YOUR_ORG/projects/YOUR_PROJECT/keys/
# NEXT_PUBLIC_SENTRY_DSN=https://your-key@o0.ingest.sentry.io/your-project-id
# SENTRY_DSN=https://your-key@o0.ingest.sentry.io/your-project-id
# SENTRY_AUTH_TOKEN=your-auth-token  # Optional, for source maps upload
# SENTRY_ORG=your-org
# SENTRY_PROJECT=your-project


# =================================================================
# Elasticsearch Configuration (Optional - for log search)
# =================================================================
# ELASTICSEARCH_URL=http://elasticsearch:9200
# ELASTICSEARCH_INDEX=logs
# ELASTICSEARCH_AUTH=false  # Set to true if authentication is required
# ELASTICSEARCH_USERNAME=elastic  # Required if ELASTICSEARCH_AUTH=true
# ELASTICSEARCH_PASSWORD=your-password  # Required if ELASTICSEARCH_AUTH=true
# ELASTICSEARCH_SSL_VERIFY=false  # Usually false for internal networks
# ELASTICSEARCH_TIMEOUT=30000  # Request timeout in milliseconds


# =================================================================
# =================================================================
# SigNoz Configuration (Optional - can be configured via UI)
# =================================================================
# SIGNOZ_ENABLED=true
# OTEL_EXPORTER_OTLP_ENDPOINT=http://signoz:4318
# OTEL_SERVICE_NAME=fitscan
# OTLP Headers - Only SigNoz API key (JSON format required)
# OTEL_EXPORTER_OTLP_HEADERS={"x-api-key":"your-signoz-api-key"}