"use client";

// Compatibility export retained so existing login-layout components do not need a
// broad rename in the same identity cutover. The rendered action is Outborn Account.
export { OutbornAccountSignInButton as AzureAdSignInButton } from './OutbornAccountSignInButton';
