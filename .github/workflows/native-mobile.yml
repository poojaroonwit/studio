name: Native mobile

on:
  pull_request:
    branches: [dev]
    paths:
      - 'mobile/**'
      - 'src/components/pwa/NativeMobileRuntime.tsx'
      - 'src/lib/native-mobile.ts'
      - 'src/lib/native-mobile-device-registry.ts'
      - 'src/lib/native-push.ts'
      - 'src/app/api/mobile/**'
      - 'src/app/ess/mobile-settings/**'
      - 'src/app/.well-known/**'
      - '.github/workflows/native-mobile.yml'
  push:
    branches: [dev]
    paths:
      - 'mobile/**'
      - 'src/components/pwa/NativeMobileRuntime.tsx'
      - 'src/lib/native-mobile.ts'
      - 'src/lib/native-mobile-device-registry.ts'
      - 'src/lib/native-push.ts'
      - 'src/app/api/mobile/**'
      - 'src/app/ess/mobile-settings/**'
      - 'src/app/.well-known/**'
      - '.github/workflows/native-mobile.yml'

permissions:
  contents: read

jobs:
  architecture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
      - run: node scripts/check-native-mobile.cjs

  android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'
      - name: Install native dependencies
        working-directory: mobile
        run: npm install --no-audit --no-fund
      - name: Generate and configure Android project
        working-directory: mobile
        env:
          NATIVE_PLATFORM: android
        run: npm run native:prepare
      - name: Compile Android debug app
        working-directory: mobile/android
        run: ./gradlew assembleDebug --stacktrace

  ios:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
      - name: Install native dependencies
        working-directory: mobile
        run: npm install --no-audit --no-fund
      - name: Generate and configure iOS project
        working-directory: mobile
        env:
          NATIVE_PLATFORM: ios
        run: npm run native:prepare
      - name: Compile iOS simulator app
        working-directory: mobile
        run: >-
          xcodebuild
          -project ios/App/App.xcodeproj
          -scheme App
          -configuration Debug
          -sdk iphonesimulator
          -destination 'generic/platform=iOS Simulator'
          CODE_SIGNING_ALLOWED=NO
          build
