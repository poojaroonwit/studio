apiVersion: v1
kind: ConfigMap
metadata:
  name: fitscan-config
  namespace: ba-fitscan
data:
  # Application Configuration
  NODE_ENV: "production"
  NEXT_TELEMETRY_DISABLED: "1"
  APP_TIMEZONE: "Asia/Bangkok"
  
  # Database Configuration (External)
  POSTGRES_DB: "firscan"
  POSTGRES_HOST: "external-postgres-host.example.com"  # Replace with your external PostgreSQL host
  POSTGRES_PORT: "5432"
  
  # Database Connection Pool Configuration
  DATABASE_SSL: "true"  # Usually true for external databases
  DATABASE_MAX_CONNECTIONS: "20"
  DATABASE_IDLE_TIMEOUT: "5000"
  DATABASE_CONNECTION_TIMEOUT: "5000"
  DATABASE_STATEMENT_TIMEOUT: "5000"
  
  # MinIO Configuration (External)
  MINIO_ENDPOINT: "external-minio-host.example.com"  # Replace with your external MinIO host
  MINIO_PORT: "9000"
  MINIO_BUCKET_NAME: "firscan"
  MINIO_USE_SSL: "true"  # Usually true for external MinIO
  
  # Migration Configuration
  MIGRATION_FAILURE_ACTION: "continue"
  SKIP_FAILED_MIGRATIONS: "true"
  DB_PUSH_FALLBACK: "true"
  SKIP_SEED: "false"
  
  # Processor Configuration
  PROCESSOR_INTERVAL_MS: "5000"
  MAX_CONCURRENT_PROCESSORS: "1"
  LOG_INTERVAL_MS: "5000"
  PROCESSOR_CONNECTION_TIMEOUT_MS: "5000"
  PROCESSOR_REQUEST_TIMEOUT_MS: "120000"
  PROCESSOR_QUIET_MODE: "true"
