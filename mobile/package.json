{
  "name": "@hrive/native-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Native iOS and Android shell for Hrive / OBSI People employee self service",
  "scripts": {
    "native:prepare": "node scripts/prepare-native.mjs",
    "native:sync": "npx cap sync",
    "native:open:ios": "npx cap open ios",
    "native:open:android": "npx cap open android",
    "native:doctor": "npx cap doctor"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
